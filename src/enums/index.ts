export enum GenderEnum {
  'man' = 0,
  'women' = 1,
  'unknown' = 2,
}

export enum AcceptEnum {
  audio = 'audio/*',
  video = 'video/*',
  image = 'image/*',
}

export enum PublicEnum {
  'public' = 1,
  'private' = 0,
}

export enum RecallTypeEnum {
  'text' = 'text',
  'video' = 'video',
}

export enum HotDetailTabEnum {
  Info = 'info',
  Activity = 'activity',
  Recalls = 'recalls',
}
